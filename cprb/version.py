"""Single source of truth for the backend version.

Bumped in lockstep with ``pyproject.toml`` and ``web/cprb/version.js`` by
``scripts/bump_version.py`` (PROTOCOL.md §8: version = code-sync signal;
docs-only changes do NOT bump).
"""

__version__ = "0.9.3"
